version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No dependencies to install for static site"
    build:
      commands:
        - echo "No build step required"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []
